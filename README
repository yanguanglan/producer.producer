# Producer

Producer is a command-line tool to pre-flight, and then release, your PHP library package. It supports Git and Mercurial for version control. It supports Github, Gitlab, and Bitbucket as origins.
